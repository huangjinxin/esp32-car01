# ESP32 智能小车 — 接线总表

> **板型**: 4D Systems GEN4-ESP32 16MB (ESP32S3-R8N16)
> **框架**: Arduino
> **USB 烧录口**: USB-C (内置 USB, GPIO19/GPIO20)
> **串口监视器**: 115200 baud

---

## 一、引脚总览

| 模块 | 设备引脚 | ESP32 GPIO | 方向 | 说明 |
|------|---------|:----------:|:----:|------|
| **L298N** | IN1 | **GPIO15** | OUT | 左侧电机 A (PWM CH0) |
| | IN2 | **GPIO16** | OUT | 左侧电机 B (PWM CH1) |
| | IN3 | **GPIO17** | OUT | 右侧电机 A (PWM CH2) |
| | IN4 | **GPIO18** | OUT | 右侧电机 B (PWM CH3) |
| | ENA / ENB | 跳线短接 | — | 由 IN 引脚 PWM 调速 |
| **巡线传感器** | VCC | 底板 **5V** | 电源 | |
| | GND | 底板 **GND** | 电源 | |
| | OUT1~OUT8 | _(见下方说明)_ | IN | 8 路红外探头 |
| **HC-SR04** | VCC | 底板 **5V** | 电源 | |
| | GND | 底板 **GND** | 电源 | |
| | Trig | **GPIO8** | OUT | 超声波触发 |
| | Echo | **GPIO9** | IN | 超声波回响 (3.3V 直连) |
| **USB-C** | D- / D+ | GPIO19 / GPIO20 | — | 烧录 + 串口监视器，不接外设 |

---

## 二、巡线传感器接线

当前只接中间 4 路进行最小化巡线控制:

| 传感器位置 | 模块 OUT 口 | ESP32 GPIO |
|:---------:|:----------:|:----------:|
| 中心偏左 2 | **OUT3** | GPIO4 |
| 中心偏左 1 | **OUT4** | GPIO5 |
| 中心偏右 1 | **OUT5** | GPIO6 |
| 中心偏右 2 | **OUT6** | GPIO7 |

> 其余 OUT1、OUT2、OUT7、OUT8 暂不接。
> 传感器 VCC → 底板 5V，GND → 底板 GND。
> 模块电位器可调节灵敏度（放在白色地面调至全灭，黑线下全亮）。

---

## 三、L298N 电机方向逻辑

> **以下为修正版**：右侧电机接线极性相反，代码已对调 IN3/IN4 电平。

### 控制信号表

| 动作 | IN1(GPIO15) | IN2(GPIO16) | IN3(GPIO17) | IN4(GPIO18) |
|------|:-----------:|:-----------:|:-----------:|:-----------:|
| **前进** (forward) | LOW | HIGH | HIGH | LOW |
| **后退** (backward) | HIGH | LOW | LOW | HIGH |
| **左转** (left) | HIGH | LOW | HIGH | LOW |
| **右转** (right) | LOW | HIGH | LOW | HIGH |
| **停止** (stop) | LOW | LOW | LOW | LOW |

### L298N 自身接线

```
L298N 模块:
  12V → 底板 12V
  GND → 底板 GND
  5V  → (无需接，板载稳压输出)

  左侧电机: OUT1 / OUT2 → 左电机正负极
  右侧电机: OUT3 / OUT4 → 右电机正负极
```

### PWM 速度

```cpp
int motorSpeed = 153;  // 约 60% (范围 0-255)
```

| 速度 | PWM 值 | 说明 |
|------|:------:|------|
| 30% | 76 | 低速巡航 |
| 60% | 153 | 适中（当前默认值） |
| 80% | 204 | 较快 |
| 100% | 255 | 全速 |

---

## 四、超声波避障

### 接线

| 超声波引脚 | 接 ESP32 / 底板 |
|-----------|----------------|
| VCC | 底板 **5V** |
| GND | 底板 **GND** |
| Trig | **GPIO8** |
| Echo | **GPIO9** (直连) |

### 避障行为

```
后退中 → 检测到障碍物 < 20cm：
  ① 电机立即停止
  ② 自动前进 1 秒（远离障碍物）
  ③ 停止

安全兜底：loop() 每 30ms 检测一次
```

### 安装位置

HC-SR04 安装于 **车尾**，朝后测距，用于倒车避障。

---

## 五、WiFi 与 Web 控制

| 项目 | 值 |
|------|-----|
| WiFi 模式 | 热点 (AP) |
| SSID | `ESP32-Car` |
| 密码 | `12345678` |
| 控制地址 | `http://192.168.4.1` |
| 串口监视器 | 115200 baud |

### Web API

| 路由 | 方法 | 参数 | 返回 |
|------|:----:|------|------|
| `/` | GET | — | HTML 控制页面 |
| `/action` | GET | `cmd=forward/backward/left/right/stop` | `200 OK` / `403 BLOCKED` |
| `/sensors` | GET | — | `[true,false,true,false]` |
| `/distance` | GET | — | `{"distance":12.3}` |

### 操作方式

```
触摸屏/鼠标：
  按住方向键 → 持续移动
  松开 → 停止
  STOP 按钮 → 立即停止

键盘：
  W / ↑        → 前进（按住持续）
  S / ↓        → 后退（按住持续）
  A / ←        → 左转（按住持续）
  D / →        → 右转（按住持续）
  空格         → 停止
  松开按键     → 停止
```

---

## 六、引脚占用速查

| GPIO | 用途 | 备注 |
|:----:|------|------|
| **0** | — | ⚠️ 启动引脚，勿用 |
| 1 | 空闲 | |
| 2 | 空闲 | |
| 3 | — | ⚠️ 启动引脚，勿用 |
| **4** | **巡线 OUT3** | |
| **5** | **巡线 OUT4** | |
| **6** | **巡线 OUT5** | |
| **7** | **巡线 OUT6** | |
| **8** | **超声波 Trig** | |
| **9** | **超声波 Echo** | |
| 10 | 空闲 | |
| 11 | 空闲 | |
| 12 | 空闲 | |
| 13 | 空闲 | |
| 14 | 空闲 | |
| **15** | **L298N IN1** | 左电机 A |
| **16** | **L298N IN2** | 左电机 B |
| **17** | **L298N IN3** | 右电机 A |
| **18** | **L298N IN4** | 右电机 B |
| **19** | **USB D-** | ⚠️ USB 内置，不接外设 |
| **20** | **USB D+** | ⚠️ USB 内置，不接外设 |
| 21 | 空闲 | |
| 26~37 | SPI Flash/PSRAM | ⛔ 芯片内部占用，勿用 |
| 45~46 | — | ⚠️ 启动引脚，勿用 |

---

## 七、烧录方式

```bash
# 编译
pio run -e 4d_systems_esp32s3_gen4_r8n16

# 上传（指定 USB 端口）
pio run -e 4d_systems_esp32s3_gen4_r8n16 \
  --target upload \
  --upload-port /dev/cu.usbmodem101

# 串口监视器
pio device monitor -p /dev/cu.usbmodem101 -b 115200
```

---

## 八、避障状态说明

| 状态 | 含义 | 电机行为 |
|------|------|---------|
| `stop` | 已停止 | 全部停止 |
| `forward` | 前进中 | 向前行驶 |
| `backward` | 后退中 | 向后倒车 |
| `left` | 左转中 | 原地左转 |
| `right` | 右转中 | 原地右转 |
| `evacuating` | 避障撤离 | 自动前进 1 秒远离障碍物 |

---

*文档版本: 2026-05-22*
