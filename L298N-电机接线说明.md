# L298N 电机驱动接线与方向说明

## 硬件接线

```
ESP32-S3    →    L298N
────────────────────────
GPIO15      →    IN1     (左侧电机 A)
GPIO16      →    IN2     (左侧电机 B)
GPIO17      →    IN3     (右侧电机 A)
GPIO18      →    IN4     (右侧电机 B)
ENA/ENB     →    跳线帽短接 (默认全速，由 IN 引脚 PWM 调速)
```

## 电机控制逻辑

L298N 每个通道由两个 IN 引脚控制：

| INx 电平 | 电机行为 |
|----------|---------|
| A=HIGH, B=LOW | 正转 |
| A=LOW,  B=HIGH | 反转 |
| A=LOW,  B=LOW  | 停止 |
| A=HIGH, B=HIGH | 刹车 |

## 四轮小车方向组合（差分驱动）

使用一个 L298N 驱动四个轮子（左右侧各两个轮子并联）：

| 动作 | IN1 (左A) | IN2 (左B) | IN3 (右A) | IN4 (右B) |
|------|:---------:|:---------:|:---------:|:---------:|
| 前进 | H | L | **L** | **H** |
| 后退 | L | H | **H** | **L** |
| 左转 | L | H | **L** | **H** |
| 右转 | H | L | **H** | **L** |
| 停止 | L | L | L | L |

> **注意**：上表中的 IN3/IN4 电平是**反接修正版**。
> 如果右侧电机接线极性正确（与左侧一致），标准组合应为：
> 前进=H,L,H,L / 后退=L,H,L,H / 左转=L,H,H,L / 右转=H,L,L,H

## 常见问题：单侧轮子反转

### 现象
点击"前进"时，左侧轮子正常正转，右侧轮子反转（或反之）。

### 原因
右侧电机的两根线在 L298N 的 OUT 端子上接反了极性。

### 解决方案

#### 方案 A：交换电机线（推荐，一劳永逸）
直接交换 L298N 的 OUT3/OUT4 端子上右侧电机的两根线（红黑对调），代码保持标准逻辑不变。

#### 方案 B：在代码中修正
在 `main.cpp` 中找到电机控制函数，对调 IN3/IN4 的电平：

```cpp
// 原版（右侧接线正确时）：
// 前进: motorWrite(HIGH, LOW, HIGH, LOW)

// 修正版（右侧接线反了）：
void motorForward() {
  motorWrite(HIGH, LOW, LOW, HIGH);   // IN3↔IN4 对调
}

void motorBackward() {
  motorWrite(LOW, HIGH, HIGH, LOW);   // IN3↔IN4 对调
}

void motorTurnLeft() {
  motorWrite(LOW, HIGH, LOW, HIGH);   // IN3↔IN4 对调
}

void motorTurnRight() {
  motorWrite(HIGH, LOW, HIGH, LOW);   // IN3↔IN4 对调
}
```

## PWM 调速说明

ENA/ENB 跳线短接时（默认全速使能），仍可通过 IN 引脚发送 PWM 信号实现调速：

```cpp
int motorSpeed = 153;  // 约 60% 速度 (0-255)

// 原理：INx=HIGH 时输出 PWM 脉冲，INx=LOW 时输出 0
void motorWrite(int in1, int in2, int in3, int in4) {
  ledcWrite(CH_LF, in1 ? motorSpeed : 0);
  ledcWrite(CH_LB, in2 ? motorSpeed : 0);
  ledcWrite(CH_RF, in3 ? motorSpeed : 0);
  ledcWrite(CH_RB, in4 ? motorSpeed : 0);
}
```

PWM 值参考：

| 速度 | PWM 值 | 说明 |
|------|--------|------|
| 10% | 25 | 可能无法启动（L298N 有启动电压阈值） |
| 30% | 76 | 低速 |
| 60% | 153 | 适中（本项目当前值） |
| 80% | 204 | 较快 |
| 100% | 255 | 全速 |

## 调试方法

1. 将小车架起（轮子悬空）
2. 上电后连接 WiFi `ESP32-Car`，密码 `12345678`
3. 浏览器打开 `http://192.168.4.1`
4. 点击方向按钮观察每个动作下四个轮子的转向

### 键盘快捷键（电脑调试）

| 按键 | 动作 |
|------|------|
| ↑ / W | 前进 |
| ↓ / S | 后退 |
| ← / A | 左转 |
| → / D | 右转 |
| 空格 | 停止 |

## WiFi 热点配置

如需修改 WiFi 名称和密码，编辑 `main.cpp` 顶部：

```cpp
const char* ssid = "ESP32-Car";        // WiFi 名称
const char* password = "12345678";     // WiFi 密码
```

---

# 巡线传感器说明

## 硬件接线

8 路巡线传感器测试接线（最少接 4 路即可测试）：

| 传感器 | ESP32-S3 |
|--------|----------|
| VCC (5V) | 5V 引脚 |
| GND | GND |
| OUT1 | GPIO4 |
| OUT2 | GPIO5 |
| OUT3 | GPIO6 |
| OUT4 | GPIO7 |
| OUT5~OUT8 | 暂不接 |

## 传感器工作原理

- 红外探头检测地面反射光
- **检测到黑线** → 输出 LOW → 网页显示绿色圆点 (■)
- **检测到白地** → 输出 HIGH → 网页显示暗色圆点 (_)
- 板载电位器可调节检测灵敏度

## 读取方式

代码通过 `/sensors` 接口返回 JSON 数组：

```
请求:  GET http://192.168.4.1/sensors
返回:  [true, false, true, false]
       true  = 检测到黑线
       false = 白色地面
```

网页端每 200ms 自动轮询，连线状态实时显示。

## 串口输出

串口监视器每 500ms 打印传感器状态：

```
传感器: ■ _ ■ _    (■=黑线, _=白地)
传感器: ■ ■ _ _
传感器: _ _ ■ ■
```

## 如何判断安装位置

1. 将传感器模块固定在小车**前部**，离地 **1~2cm**
2. 放在白色地面 → 网页上显示全部 `_`
3. 放一张黑胶带在其中一个探头下 → 对应位置显示 `■`
4. 调整电位器直到检测稳定

## 代码配置

如需修改传感器引脚或数量，编辑 `main.cpp`：

```cpp
#define SENSOR_COUNT  4                    // 使用的传感器数量
const int sensorPins[SENSOR_COUNT] = { 4, 5, 6, 7 };  // GPIO 引脚
static const bool SENSOR_ACTIVE_LOW = true;  // true=黑线输出LOW, false=黑线输出HIGH
```
